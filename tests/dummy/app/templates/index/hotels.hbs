<MapRoute as |map|>
  {{#each @model as |marker|}}
    <map.marker
      @id={{marker.id}}
      @lat={{marker.lat}}
      @lng={{marker.lng}}
      @onClick={{action "onClick" marker}} />
  {{/each}}
  <map.outlet @components={{map}} />
</MapRoute>