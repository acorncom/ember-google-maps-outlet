<GMapRoute as |g|>
  {{#each @model as |marker|}}
    <g.marker
        @lat={{marker.lat}}
        @lng={{marker.lng}}
        @onClick={{action "onClick" marker}} />
  {{/each}}
  {{g.outlet g=g}}
</GMapRoute>